<!-- wrap around page-->
	<div class ="jumbotron center">
		<h2><p>Wellcome</p></h2>
		
			<% @posts.each do |post| %>
				<h4><%= link_to post.title, post_path(post) %></h4>
				<%= link_to 'Edit', edit_post_path(post) %>
				<%= link_to 'Delete', delete_post_path(post), method: :delete, data: {confirm: 'Are you sure?'} %>
			<% end %>
	</div>	



